/**
 * `localStorage` key written by desktop builds before the TaskForge rename.
 * Migrated to `taskforge_onboarding_done` on startup in `AppShellComponent`.
 */
export const LEGACY_ONBOARDING_DONE_STORAGE_KEY = 'autodesk_onboarding_done';
